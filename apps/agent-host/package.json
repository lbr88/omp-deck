{
	"name": "@omp-deck/agent-host",
	"version": "0.7.0",
	"private": true,
	"type": "module",
	"description": "omp extension that turns a machine into a remote agent host for omp-deck. Runs inside `omp --mode rpc`; no binary build, no runtime dependencies beyond the omp SDK.",
	"main": "./src/index.ts",
	"scripts": {
		"typecheck": "tsc --noEmit",
		"test": "bun test"
	},
	"dependencies": {
		"@oh-my-pi/pi-coding-agent": "15.1.7",
		"@omp-deck/protocol": "workspace:*"
	},
	"devDependencies": {
		"@types/bun": "^1.3.14",
		"typescript": "^5.6.3"
	},
	"engines": {
		"bun": ">=1.3.14"
	}
}
