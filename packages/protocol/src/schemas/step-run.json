{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://omp-deck.app/schemas/step-run.json",
  "title": "RoutineStep — run",
  "description": "Shell command. cmd /c on Windows, bash -lc on POSIX. Templated arg substitution applied to `command` and `cwd` before exec.",
  "type": "object",
  "unevaluatedProperties": false,
  "allOf": [
    { "$ref": "https://omp-deck.app/schemas/step-common.json" },
    {
      "type": "object",
      "required": ["type", "command"],
      "properties": {
        "type": { "const": "run" },
        "command": {
          "type": "string",
          "minLength": 1,
          "description": "Templated. Whole-string substitution; no shell metacharacter escaping applied (use deterministic templating or quote in the command itself)."
        },
        "cwd": {
          "type": "string",
          "description": "Working directory. Templated. Defaults to the routine's cwd if unset."
        }
      }
    }
  ]
}
