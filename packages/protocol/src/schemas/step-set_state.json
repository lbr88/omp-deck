{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://omp-deck.app/schemas/step-set_state.json",
  "title": "RoutineStep — set_state",
  "description": "Atomically upsert key/value pairs into the routine's cross-run state. Values are templated; they may reference any prior step's output.",
  "type": "object",
  "unevaluatedProperties": false,
  "allOf": [
    { "$ref": "https://omp-deck.app/schemas/step-common.json" },
    {
      "type": "object",
      "required": ["type", "state"],
      "properties": {
        "type": { "const": "set_state" },
        "state": {
          "type": "object",
          "minProperties": 1,
          "description": "Key/value pairs. Templates in values are rendered before the upsert. Reads at next-run start populate context.state.<key>."
        }
      }
    }
  ]
}
