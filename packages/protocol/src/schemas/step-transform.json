{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://omp-deck.app/schemas/step-transform.json",
  "title": "RoutineStep — transform",
  "description": "Pure-JS expression over the run context. Evaluated in a quickjs sandbox; no network, no fs, no timers > 100ms wall clock. Result captured to steps.<id>.json.",
  "type": "object",
  "unevaluatedProperties": false,
  "allOf": [
    { "$ref": "https://omp-deck.app/schemas/step-common.json" },
    {
      "type": "object",
      "required": ["type", "body"],
      "properties": {
        "type": { "const": "transform" },
        "body": {
          "type": "string",
          "minLength": 1,
          "description": "JS source. The expression's return value is captured. Common pattern: `return context.steps.X.json.filter(item => ...)`."
        }
      }
    }
  ]
}
